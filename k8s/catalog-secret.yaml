apiVersion: v1
kind: Secret
metadata:
  name: catalog-secrets
  namespace: anloca
type: Opaque
stringData:
  # Replace the value with your real Postgres connection string
  database-url: "REPLACE_WITH_CATALOG_DATABASE_URL"
