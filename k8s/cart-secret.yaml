apiVersion: v1
kind: Secret
metadata:
  name: cart-secrets
  namespace: anloca
type: Opaque
stringData:
  # Replace the value with your real Postgres connection string
  database-url: "REPLACE_WITH_CART_DATABASE_URL"
